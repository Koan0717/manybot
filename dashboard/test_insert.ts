import { getPool } from './src/lib/db';

async function main() {
    try {
        const guildId = '1502700570396590100'; // I need a valid guild id, let me just try fetching one from the bot's DB using python, wait.
        // Actually, let's just query the master DB to find a guild ID.
    } catch (e) {
        console.error(e);
    }
}
main();
